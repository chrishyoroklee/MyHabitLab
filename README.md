# MyHabitLab

Brief starter info for developers working on the project.

## Project root
Work out of the repository root:

```
/Users/hyorok/Desktop/Projects/MyHabitLab
```

The main app target lives in `MyHabitLab/` and the Xcode project is `MyHabitLab.xcodeproj`.
